namespace LOGGER
{
    /// <summary>
    /// Represents the log levels used to categorize log messages by their severity.
    /// </summary>
    public enum LogLevel
    {
        FATAL,
        ERROR,
        WARN,
        INFO,
        DEBUG
    }
}
